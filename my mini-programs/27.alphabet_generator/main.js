function alphaGen() {
  let strUpper = "";
  for (let i = 65; i < 91 ; i++) {
    strUpper += String.fromCodePoint(i);
  }

  let strLower = "";
  for (let j = 97; j < 123 ; j++) {
    strLower += String.fromCodePoint(j);
  }
}

